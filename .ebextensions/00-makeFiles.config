files:
  "/sbin/appstart":
    mode: "000755"
    owner: webapp
    group: webapp
    content: |
      #!/usr/bin/env bash
      JAR_PATH=/var/app/current/application.jar

      # run application
      killall java
      java -Dfile.encoding=UTF-8 -DSpring.profiles.active=production -jar $JAR_PATH
